module hello_world

import "std/io" import "std/math"

// This is a comment
fn main() {
    str := "Hello, World!"
    i := 0
    i = i + 1
    io.printf("%s\n", str) // This is a comment after some tokens
    io.printf("%d\n", i)
}
